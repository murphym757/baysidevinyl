<!-- Navigation -->
<nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
  <div class="container">
      <div class="navbar-header">

          <% if user_signed_in? %>
          <a class="navbar-brand" href="#">
              <%= link_to "Hello, User", pages_path, class: "navbar-brand" %>
          </a>
          <% else %>
          <a class="navbar-brand" href="#">
              <%= link_to "Hello, Guest", pages_path, class: "navbar-brand" %>
          </a>
          <% end %>

          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
              Menu <i class="fa fa-bars"></i>
          </button>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
          <ul class="nav navbar-nav">
             <% if user_signed_in? %>
              <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
              <li class="hidden">
                  <a href="#index"></a>
              </li>
              <<li><%= link_to "My Record Collection", new_user_registration_path, class: "navbar-brand" %></li>
              <% else %>
              <li><%= link_to "Sign Up", new_user_registration_path, class: "navbar-brand" %></li>
              <li><%= link_to "Sign In", new_user_session_path, class: "navbar-brand" %></li>
            <% end %>
          </ul>
      </div>
      <!-- /.navbar-collapse -->

  </div>
  <!-- /.container -->
</nav>
